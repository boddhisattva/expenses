<title><%= full_title(yield(:title)) %> </title>
<div class="container">
  <h1> Expenses App </h1>
    <% if logged_in? %>
      <%= link_to "Log out", logout_path, method: "delete", style:"float:right" %>
    <% end %>
</div>
